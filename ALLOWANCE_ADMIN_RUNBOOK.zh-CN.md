# StoriesLens 额度后台与邀请码操作手册

## 已实现的体系

后台入口是 `/admin-allowances`，页面本身不在公开导航中，并设置了 `noindex`。后台可以：

- 按中国大陆、美国、其他国家或地区生成不同邀请码。
- 为邀请码绑定免费体验、个人故事、熟人共创、教师班级或电影额度包。
- 设置生成数量、每个码可使用人数、有效期，并随时停用整批未使用邀请码。
- 查找已认证用户并人工补发额度。
- 查看额度发放、预占、成功扣减、失败退回和实际模型成本流水。
- 导出邀请码 CSV；邀请码明文只显示一次，服务器只保存不可逆指纹。

## 额度不是模糊的“积分”

用户看到的都是可理解的成果单位：

| 额度 | 用户理解 | 扣减时机 |
| --- | --- | --- |
| 故事项目 | 新建一本故事 | 项目安全创建成功后 |
| AI 插图 | 生成一张插图 | 图片生成且安全审核通过后 |
| 5秒电影镜头 | 生成一个5秒片段 | 视频完成并可下载后 |
| 受邀共创者 | 邀请一位家人或朋友 | 共创者正式加入后（后续接入） |
| 班级项目 | 建立一本班级作品 | 班级项目创建成功后（后续接入） |
| 学生作品 | 收录一份学生作品 | 作品进入班级书后（后续接入） |

昂贵操作先“预占”额度；成功后才扣减，失败或超时会自动归还。重复请求使用幂等键，避免网络重试造成重复扣费。

## 当前额度包

- 免费体验包：$0 / ¥0；1个故事项目、3张 AI 插图，每个账户只自动发放一次。
- 个人故事包：$19 / ¥129；1个故事项目、12张 AI 插图。
- 熟人共创包：$39 / ¥269；1个故事项目、18张 AI 插图、5位受邀共创者。
- 教师班级项目：$79 / ¥569；1个班级项目、30份学生作品。
- 30秒电影包：$39 / ¥269；6个5秒电影镜头。
- 60秒电影包：$69 / ¥499；12个5秒电影镜头。

创始人带领的 Story Squad 工作坊不是自助额度包：价格为 $129 / ¥899 每位，先收 $49 / ¥349 席位订金，达到5人后确认开班并收余款。

金额与额度是分开的：未来 Stripe 或微信支付成功后，由经过验签的服务端 webhook 用订单号作为幂等键发放对应额度。绝不能由“支付成功页”或浏览器自行加额度。

## 内测最简单的发放方法

1. 在后台选择地区和额度包。
2. 第一轮建议每个码只允许1人使用、有效30天。
3. 生成后立即下载 CSV，并在自己的私密表格里记录：受邀人、地区、发送日期、邀请码、是否激活。
4. 私下收款的用户，注册后通过姓名或脱敏联系方式找到其账户，再补发已购买的额度包。
5. 如果邀请码外泄，停用该批次；已领取到的额度不会被误删。

建议第一批每个地区只生成10个一人一码。不要使用一个可供很多人共享的万能码，否则无法追踪来源、阻止转发或计算转化率。

## 生产环境安全配置

生成一个不少于24位的随机后台密钥，只把它的 SHA-256 摘要配置到服务器：

```bash
printf '%s' '在这里输入你的长随机密钥' | shasum -a 256
```

生产环境设置：

```text
ADMIN_ACCESS_KEY_SHA256=上一步得到的64位摘要
PLATFORM_DATA_DIR=/受保护的持久化磁盘/storieslens-data
```

要求：

- 原始后台密钥只保存在密码管理器，不写进 Git、网页代码、聊天记录或共享表格。
- 数据目录必须是持久化、加密、可每日备份的磁盘；不能使用每次部署都会清空的临时目录。
- 后台只通过 HTTPS 访问；连续输错会触发限流，登录 cookie 为 HttpOnly、SameSite=Strict，12小时失效。
- 备份至少保留7个每日版本；每月做一次恢复演练。
- 离职、设备丢失或怀疑泄露时立即更换后台密钥。

## 当前上线边界

`storieslens.com` 目前的 GitHub Pages 只能托管静态网页，不能安全运行此 Node 后台，也不能持久保存额度账本。因此本地功能已经完成并通过测试，但正式域名启用前，还必须把 Node 服务部署到带持久磁盘或数据库的后端环境，并把 `/api/*` 路由接到该服务。

在完成后端托管、HTTPS、验证码发送、地区存储与备份前，不应对外宣称“线上额度后台已经启用”。
