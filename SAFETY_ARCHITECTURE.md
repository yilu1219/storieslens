# StoriesLens 年龄、监护与内容安全架构

## 不能做出的承诺

任何敏感词表、AI 审核模型或图片生成平台都不能保证 100% 识别所有变体。因此 StoriesLens 不应宣传“绝对不会出现任何不安全内容”，而应采用多层防御、故障关闭、持续测试和人工复核。

## 当前已经实现的安全闸门

### 1. 年龄分组

- `18 岁以下`：必须由成年人确认监督后才能继续。
- `18 岁或以上`：可独立创建与购买。
- 不收集精确生日，减少不必要的个人信息。

当前本地测试版的监护确认只保存在设备中，不能证明确认者真实身份。正式开放儿童账号前，必须升级为经过验证的成人账号与子账号关系。

### 2. 浏览器端即时阻止

首页灵感、Story DNA、故事火花及作品操作会先进行本地安全检查。命中色情、血腥暴力、自残或危险制作内容时，界面立即停止继续操作。

浏览器端规则只用于即时反馈，不能作为唯一防线，因为用户可以绕过浏览器直接调用接口。

### 3. 服务端强制审核

所有 AI 文本、图片和视频接口都在服务器再次审核。服务端不会相信前端传来的“已审核”标记。

- AI 文本：输入审核 → 模型安全指令 → 输出审核。
- 图片：文字提示审核 → 图片生成平台审核 → 成图再次进行图片审核 → 通过后才返回。
- 任一外部审核服务不可用时，文本和媒体默认停止，不冒险放行。
- 被拦截的本地成图会被删除，不进入作品库。

外部审核使用 `omni-moderation-latest`，覆盖色情、未成年人色情、暴力、血腥暴力、自残、仇恨、威胁及危险违法内容。StoriesLens 采用比单一 `flagged` 更严格的类别分数阈值。

### 4. 视频默认关闭

当前审核模型不直接支持视频输入。仅审核文字和第一帧不足以保证整段视频安全，因此：

```text
SAFE_VIDEO_GENERATION_ENABLED=false
```

正式开启 AI 电影前，需要增加：视频逐帧抽样、每帧图片审核、音频转写审核、失败成片隔离、人工复核与发布审批。完成以前不允许在公开产品中开启视频生成。

## 正式儿童账号必须增加

1. 成年人创建主账号并验证邮箱。
2. 成年人创建受监管的未成年人子账号，而不是让儿童自行勾选同意。
3. 保存可审计的监护同意时间、政策版本与撤回记录。
4. 家长面板可以查看项目、成员、邀请、分享、媒体生成和购买。
5. 默认私密；未成年人不能创建公开主页、接收陌生人私信或被公开搜索。
6. 所有小队邀请需要家长/教师批准，并可随时移除成员。
7. 对儿童内容设置最短保存期限与删除机制，日志不保存不必要的原始故事正文。
8. 发布前由熟悉目标市场儿童隐私法规的律师审核流程和政策。

## 环境变量

```text
OPENAI_MODERATION_API_KEY=
OPENAI_MODERATION_MODEL=omni-moderation-latest
REQUIRE_EXTERNAL_TEXT_MODERATION=true
REQUIRE_EXTERNAL_MEDIA_MODERATION=true
SAFE_VIDEO_GENERATION_ENABLED=false
```

审核密钥只放在服务器环境变量，绝不能写进浏览器 JavaScript 或提交到公开代码仓库。

## 安全测试基线

每次发布前至少覆盖：

- 中英文直接敏感词。
- 空格、符号、大小写和 Unicode 变体。
- 隐晦色情与未成年人色情请求。
- 非血腥冲突与血腥暴力的边界。
- 自残意图与危险操作指令。
- 安全的童话冲突、历史讨论和健康教育误报。
- 外部审核超时、密钥缺失和接口失败时是否确实停止生成。

安全拦截率、误报率和人工复核结果应单独记录，但不要把儿童原始内容写入分析日志。
